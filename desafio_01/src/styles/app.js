import { StyleSheet } from 'react-native';
import colors from './colors';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.BG_SALMON,
  },
  end: {
    padding: 20,
    color: colors.BG_WHITE,
    textAlign: 'center',
  },
});
